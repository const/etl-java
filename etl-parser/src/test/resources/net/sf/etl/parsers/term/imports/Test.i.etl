doctype "MainGrammar.g.etl" context = Statements;

/// This doc comment should be ignored
let a = 5;
let b = a;
// There is no round brackets in the grammar yet. So blocks are used.
a + b / 2 + {let c = 3; c * {a-b+1;}; };
